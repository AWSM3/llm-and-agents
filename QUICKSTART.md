# Quick Start Guide

## 🚀 Быстрый старт

### Запуск проекта локально

```bash
# 1. Перейдите в директорию проекта
cd ai-presentation

# 2. Установите зависимости (если еще не установлены)
npm install

# 3. Запустите dev сервер
npm run dev
```

Откройте браузер по адресу [http://localhost:3000](http://localhost:3000)

## 📋 Основные команды

```bash
# Development сервер
npm run dev

# Production сборка
npm run build

# Запуск production сервера
npm start

# Линтинг
npm run lint

# Type checking
npm run type-check
```

## 🎮 Управление

### Клавиатура
- **←/→** - Навигация между слайдами
- **Home** - Переход к началу (Hero)
- **End** - Переход к последнему слайду
- **Escape** - Закрыть модальное окно

### Touch
- **Swipe влево** - Следующий слайд
- **Swipe вправо** - Предыдущий слайд

### Мышь/Тач
- Клик по индикаторам внизу
- Клик по кнопкам навигации
- Клик по пунктам меню

## 📁 Структура

```
ai-presentation/
├── app/                  # Next.js App Router
│   ├── layout.tsx       # Root layout
│   ├── page.tsx         # Main page
│   └── globals.css      # Global styles
├── components/          # React компоненты
│   ├── hero/           # Hero section
│   ├── navigation/     # Navigation components
│   ├── slides/         # Slide components
│   ├── charts/         # Chart components
│   ├── modals/         # Modal dialogs
│   └── ui/             # shadcn/ui components
└── lib/                # Utilities
    └── content-data.ts # Content data
```

## ✏️ Редактирование контента

Весь контент находится в одном файле:

**`lib/content-data.ts`**

Отредактируйте объект `contentData` для изменения:
- Текста слайдов
- Данных моделей
- Информации об инструментах
- И всего остального контента

Пример:
```typescript
export const contentData: ContentData = {
  hero: {
    title: "Ваш новый заголовок",
    subtitle: "Ваш новый подзаголовок",
    // ...
  },
  // ...
}
```

## 🎨 Кастомизация стилей

### Цвета

Отредактируйте `app/globals.css`:

```css
:root {
  --color-teal-500: 33 128 141;    /* Primary color */
  --color-brown-600: 94 82 64;     /* Secondary color */
  /* ... другие цвета */
}
```

### Компоненты

Все компоненты можно найти в `components/` и легко модифицировать.

## 📊 Добавление нового слайда

1. Создайте компонент в `components/slides/NewSlide.tsx`
2. Добавьте данные в `lib/content-data.ts`
3. Импортируйте и добавьте в `app/page.tsx`

## 🐛 Troubleshooting

### Порт занят?
```bash
npm run dev -- -p 3001  # Используйте другой порт
```

### Ошибки зависимостей?
```bash
rm -rf node_modules package-lock.json
npm install
```

### TypeScript ошибки?
```bash
npm run type-check  # Проверка типов
```

## 📚 Полезные ресурсы

- [Next.js Documentation](https://nextjs.org/docs)
- [shadcn/ui Documentation](https://ui.shadcn.com)
- [Tailwind CSS](https://tailwindcss.com)
- [Recharts](https://recharts.org)

## 🚢 Deployment

См. [DEPLOYMENT.md](./DEPLOYMENT.md) для детальных инструкций.

Быстрый деплой на Vercel:
```bash
npm i -g vercel
vercel
```

## ❓ Нужна помощь?

- Проверьте [README.md](./README.md)
- Посмотрите [MIGRATION.md](./MIGRATION.md)
- Изучите [DEPLOYMENT.md](./DEPLOYMENT.md)

